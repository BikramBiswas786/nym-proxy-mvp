import express from 'express';import cors from 'cors';import crypto from 'crypto';const app=express();const cache=new Map();setInterval(()=>{const now=Date.now();for(const[k,v]of cache){if(now>v.exp)cache.delete(k)}},10*60*1000);app.use(cors());app.use(express.json({limit:'5mb'}));app.use(express.static('public'));app.get('/v1/health',(r,s)=>s.json({ok:true}));app.post('/v1/proxy',async(r,s)=>{const url=r.body?.url;if(!url)return s.json({error:'URL required'});try{const res=await fetch(url,{headers:{'User-Agent':'Mozilla/5.0'},timeout:20000});const html=await res.text();const t=crypto.randomBytes(16).toString('hex');cache.set(t,{h:html,exp:Date.now()+4*60*60*1000});s.json({success:true,token:t,size:html.length})}catch(e){s.json({error:e.message})}});app.get('/v1/proxy/view/:t',(r,s)=>{const c=cache.get(r.params.t);if(!c||Date.now()>c.exp)return s.json({error:'Expired'});s.set('Content-Type','text/html').send(c.h)});export default app;
